[project]
name = "deepfake-sentinel-backend"
version = "1.0.0"
description = "Evidence-driven media authenticity screening API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

